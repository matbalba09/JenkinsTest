pipeline {
    
    agent any    
    
    stages {
        stage("versions") {
        
            steps {
                sh 'node -v'
                sh 'npm -v'
            }   
        }
    }
}
